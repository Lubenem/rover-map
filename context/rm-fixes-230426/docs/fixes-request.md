# Fixes Request

Please pass the following requirements to the candidate:

1. The test assignment must be completed using `ROS2 + Gazebo Harmonic + PX4`.
2. Fix `Fast-LIVO2` so the output is in color, not grayscale.
